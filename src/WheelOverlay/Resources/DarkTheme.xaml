<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Dark Theme Colors for WheelOverlay v0.6.0 -->
    <SolidColorBrush x:Key="ThemeBackground" Color="#1E1E1E" />
    <SolidColorBrush x:Key="ThemeForeground" Color="#E0E0E0" />
    <SolidColorBrush x:Key="ThemeControlBackground" Color="#2D2D2D" />
    <SolidColorBrush x:Key="ThemeControlBorder" Color="#555555" />
    <SolidColorBrush x:Key="ThemeControlForeground" Color="#E0E0E0" />
    <SolidColorBrush x:Key="ThemeSubtext" Color="#B0B0B0" />
    <SolidColorBrush x:Key="ThemeAccent" Color="#4CC2FF" />
    <Color x:Key="ThemeDropShadow">#40000000</Color>

    <!-- Implicit control styles so all controls follow the theme -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource ThemeControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ThemeControlForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorder}" />
        <Setter Property="CaretBrush" Value="{DynamicResource ThemeControlForeground}" />
    </Style>

    <!-- ComboBox toggle button template for dark mode -->
    <ControlTemplate x:Key="DarkComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Border x:Name="Border" Grid.ColumnSpan="2" Background="{DynamicResource ThemeControlBackground}"
                    BorderBrush="{DynamicResource ThemeControlBorder}" BorderThickness="1" CornerRadius="2" />
            <Border Grid.Column="0" Background="Transparent" Margin="1" />
            <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource ThemeControlForeground}" />
        </Grid>
    </ControlTemplate>

    <!-- Dark ComboBox style with custom template -->
    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource ThemeControlForeground}" />
        <Setter Property="Background" Value="{DynamicResource ThemeControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorder}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton" Template="{StaticResource DarkComboBoxToggleButton}"
                                      Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press" />
                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="6,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource ThemeControlForeground}" />
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Grid Name="DropDown" SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder" Background="{DynamicResource ThemeControlBackground}"
                                        BorderBrush="{DynamicResource ThemeControlBorder}" BorderThickness="1" CornerRadius="2" />
                                <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{DynamicResource ThemeControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ThemeControlForeground}" />
        <Setter Property="Padding" Value="4,2" />
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource ThemeAccent}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorder}" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ThemeControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ThemeControlForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorder}" />
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{DynamicResource ThemeAccent}" />
    </Style>

</ResourceDictionary>
