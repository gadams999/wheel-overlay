<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- ================================================================ -->
    <!-- MaterialSettingsWindow — side-navigation ListBox style           -->
    <!-- ================================================================ -->

    <!-- ListBox for the left navigation panel (sits inside a ColorZone) -->
    <Style x:Key="NavListBoxStyle" TargetType="ListBox">
        <Setter Property="Width" Value="180"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
        <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Contained"/>
    </Style>

<!-- Content area border (right panel) -->
    <Style x:Key="ContentBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <!-- Settings Window style -->
    <Style x:Key="MaterialSettingsWindowStyle" TargetType="Window">
        <Setter Property="MinWidth" Value="680"/>
        <Setter Property="MinHeight" Value="480"/>
        <Setter Property="Background" Value="{DynamicResource MaterialDesignBackground}"/>
        <Setter Property="WindowStartupLocation" Value="CenterScreen"/>
    </Style>

</ResourceDictionary>
