<Window x:Class="OpenDash.OverlayCore.Settings.MaterialSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        MinWidth="680" MinHeight="480"
        Width="760" Height="540"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ThemeBackground}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/MaterialStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: navigation panel -->
        <Border Grid.Column="0"
                Background="{DynamicResource ThemeControlBackground}"
                BorderBrush="{DynamicResource ThemeControlBorder}"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Settings"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="16,16,16,10"
                           Foreground="{DynamicResource ThemeForeground}"/>
                <ListBox x:Name="CategoryListBox"
                         Style="{StaticResource NavListBoxStyle}"
                         ItemContainerStyle="{StaticResource NavListBoxItemStyle}"
                         Margin="4,0,4,8"
                         SelectionChanged="CategoryListBox_SelectionChanged"/>
            </DockPanel>
        </Border>

        <!-- Right: content + buttons -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Category content area -->
            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="20,16,20,0"
                          PreviewMouseWheel="ContentScrollViewer_PreviewMouseWheel">
                <ContentControl x:Name="CategoryContent"/>
            </ScrollViewer>

            <!-- OK / Apply / Cancel -->
            <Border Grid.Row="1"
                    BorderBrush="{DynamicResource ThemeControlBorder}"
                    BorderThickness="0,1,0,0"
                    Padding="16,10">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="OkButton"
                            Content="OK" Width="80" Margin="0,0,8,0"
                            Click="OkButton_Click"
                            Background="{DynamicResource ThemeControlBackground}"
                            Foreground="{DynamicResource ThemeControlForeground}"
                            BorderBrush="{DynamicResource ThemeControlBorder}"/>
                    <Button x:Name="ApplyButton"
                            Content="Apply" Width="80" Margin="0,0,8,0"
                            Click="ApplyButton_Click"
                            Background="{DynamicResource ThemeControlBackground}"
                            Foreground="{DynamicResource ThemeControlForeground}"
                            BorderBrush="{DynamicResource ThemeControlBorder}"/>
                    <Button x:Name="CancelButton"
                            Content="Cancel" Width="80"
                            Click="CancelButton_Click"
                            Background="{DynamicResource ThemeControlBackground}"
                            Foreground="{DynamicResource ThemeControlForeground}"
                            BorderBrush="{DynamicResource ThemeControlBorder}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
