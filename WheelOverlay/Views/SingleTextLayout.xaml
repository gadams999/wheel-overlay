<UserControl x:Class="WheelOverlay.Views.SingleTextLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WheelOverlay.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:OverlayViewModel}">
    <UserControl.Resources>
        <Style x:Key="FlashingTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Settings.SelectedTextColor}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDeviceNotFound}" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsDisplayingEmptyPosition}" Value="True">
                    <Setter Property="Foreground" Value="{Binding Settings.NonSelectedTextColor}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsFlashing}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation 
                                    Storyboard.TargetProperty="Opacity"
                                    From="1.0"
                                    To="0.3"
                                    Duration="0:0:0.125"
                                    AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation 
                                    Storyboard.TargetProperty="Opacity"
                                    To="1.0"
                                    Duration="0:0:0.05"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="AnimationContainer" ClipToBounds="True">
        <!-- Current Text (animating out) -->
        <TextBlock x:Name="CurrentText"
                   Text="{Binding DisplayedText}"
                   FontSize="{Binding Settings.FontSize}"
                   FontFamily="{Binding Settings.FontFamily}"
                   FontWeight="Bold"
                   TextOptions.TextRenderingMode="Aliased"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Style="{StaticResource FlashingTextStyle}"
                   RenderTransformOrigin="0.5,0.5">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.8"/>
            </TextBlock.Effect>
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <RotateTransform x:Name="CurrentRotate"/>
                    <TranslateTransform x:Name="CurrentTranslate"/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
        
        <!-- Next Text (animating in) -->
        <TextBlock x:Name="NextText"
                   Text=""
                   FontSize="{Binding Settings.FontSize}"
                   FontFamily="{Binding Settings.FontFamily}"
                   FontWeight="Bold"
                   TextOptions.TextRenderingMode="Aliased"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Style="{StaticResource FlashingTextStyle}"
                   Opacity="0"
                   RenderTransformOrigin="0.5,0.5">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.8"/>
            </TextBlock.Effect>
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <RotateTransform x:Name="NextRotate"/>
                    <TranslateTransform x:Name="NextTranslate"/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>
