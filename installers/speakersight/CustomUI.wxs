<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs"
     xmlns:ui="http://wixtoolset.org/schemas/v4/wxs/ui">

  <!-- UI declarations moved into Package.wxs to ensure Fragment linking. This file is retained
       so the build command signature stays consistent across overlays. -->
  <Fragment />

</Wix>
