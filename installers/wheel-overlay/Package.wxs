<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs"
     xmlns:ui="http://wixtoolset.org/schemas/v4/wxs/ui">

  <Package
    Name="WheelOverlay"
    Manufacturer="Gavin Adams"
    Version="0.7.0"
    UpgradeCode="051FF004-39FE-4DD8-ABA0-238C8E12AFAF"
    Compressed="yes">

    <!-- Enforce in-place upgrades; block downgrades with clear message -->
    <MajorUpgrade
      DowngradeErrorMessage="A newer version of WheelOverlay is already installed. Please uninstall it before installing this version." />

    <MediaTemplate EmbedCab="yes" />

    <!-- Installation directory: Program Files\OpenDash\WheelOverlay -->
    <StandardDirectory Id="ProgramFilesFolder">
      <Directory Id="OpenDashDir" Name="OpenDash">
        <Directory Id="INSTALLFOLDER" Name="WheelOverlay" />
      </Directory>
    </StandardDirectory>

    <!-- Start Menu shortcut directory -->
    <StandardDirectory Id="ProgramMenuFolder">
      <Directory Id="ApplicationProgramsFolder" Name="WheelOverlay" />
    </StandardDirectory>

    <!-- Desktop shortcut directory -->
    <StandardDirectory Id="DesktopFolder" />

    <!-- Application files (populated by generate_components.ps1 / build_msi.ps1) -->
    <Feature Id="MainApplication" Title="WheelOverlay" Level="1">
      <ComponentGroupRef Id="AppComponents" />
      <ComponentRef Id="StartMenuShortcut" />
      <ComponentRef Id="DesktopShortcut" />
    </Feature>

    <!-- Start Menu shortcut -->
    <Component Id="StartMenuShortcut" Directory="ApplicationProgramsFolder" Guid="*">
      <Shortcut
        Id="StartMenuShortcut"
        Name="WheelOverlay"
        Description="Sim racing rotary encoder overlay"
        Target="[INSTALLFOLDER]WheelOverlay.exe"
        WorkingDirectory="INSTALLFOLDER"
        Icon="AppIcon" />
      <RemoveFolder Id="CleanUpStartMenuFolder" Directory="ApplicationProgramsFolder" On="uninstall" />
      <RegistryValue
        Root="HKCU"
        Key="Software\Gavin Adams\WheelOverlay"
        Name="StartMenuShortcutInstalled"
        Type="integer"
        Value="1"
        KeyPath="yes" />
    </Component>

    <!-- Desktop shortcut -->
    <Component Id="DesktopShortcut" Directory="DesktopFolder" Guid="*">
      <Shortcut
        Id="DesktopShortcut"
        Name="WheelOverlay"
        Description="Sim racing rotary encoder overlay"
        Target="[INSTALLFOLDER]WheelOverlay.exe"
        WorkingDirectory="INSTALLFOLDER"
        Icon="AppIcon" />
      <RegistryValue
        Root="HKCU"
        Key="Software\Gavin Adams\WheelOverlay"
        Name="DesktopShortcutInstalled"
        Type="integer"
        Value="1"
        KeyPath="yes" />
    </Component>

    <!-- Install directory shown in the WixUI_InstallDir directory browse dialog -->
    <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER" />

    <!-- Icon shown in Add/Remove Programs -->
    <Property Id="ARPPRODUCTICON" Value="AppIcon" />

    <!-- App icon for shortcuts and Add/Remove Programs -->
    <Icon Id="AppIcon" SourceFile="app.ico" />

    <!-- UI must live inside Package so it is included in the link graph (Fragment is dead-code-eliminated) -->
    <WixVariable Id="WixUILicenseRtf" Value="LICENSE.rtf" />
    <ui:WixUI Id="WixUI_InstallDir" />

  </Package>

  <!-- APPLICATION_FILES_START -->
  <!-- APPLICATION_FILES_END -->

</Wix>
